# Chat Commands

## Msg

the `/msg` command allows you to send a private message to another player.

Usage:
```
/msg player message
```
Where `player` is the username of the player your message will be sent to, and `message` and eveything after it is the content of your message.

